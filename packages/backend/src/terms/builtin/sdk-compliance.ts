/**
 * 内置《SDK 合规性文档》模板。
 *
 * 主要读者是接入方开发者：他们要据此在自己的隐私政策里逐项披露，所以字段、上限、
 * 开关与限流值必须与实现完全一致，写虚了等于让接入方披露失实。最终用户同样可读。
 *
 * 与 privacy-policy.ts 分工：那份讲数据到服务端之后「怎么处理」，这份讲 SDK
 * 「收集什么、怎么收集、怎么关」。
 *
 * 正文逐项对应实现，改任何一处都要回来对：
 * - 请求头与探测规则：四个 SDK 的 http 层（sanitizePlatformVersion 与
 *   detectPlatform / detectPlatformVersion）
 * - 字段与上限：CreateFeedbackDto / UploadLogDto / IngestEventsDto /
 *   CheckVersionUpdateDto
 * - 事件采集的本地状态与退出机制：四个 SDK 的 analytics 模块
 *   （sdk/typescript/src/analytics.ts、sdk/vanilla-js/verhub-sdk.js 的 EventQueue、
 *   sdk/python/verhub_sdk/_analytics.py、sdk/rust/src/analytics.rs）
 * - 服务端补录的来源字段：ClientOriginService.describe
 * - IP 匿名化：events/ip-anonymize.ts 与 VERHUB_EVENT_IP_STORAGE
 * - 限流：app.module.ts 的 VERHUB_PUBLIC_RATE_LIMIT（默认 300/分钟）与
 *   feedback-forward-throttler.ts（默认 3/小时）
 * - 去重：common/dedup.ts（SHA-256 截 128 位，默认 60 秒窗口）——**不含事件采集**，
 *   事件靠客户端幂等键 event_id 精确去重
 * - 数据主体权利端点：EventsController 的 /public/{projectKey}/events/me
 */

/** 内置正文的最后修订时间（Unix 秒）。每次实质性修订都要一并改这个值。 */
export const SDK_COMPLIANCE_UPDATED_AT = 1787529600 // 2026-08-24T00:00:00Z

export const SDK_COMPLIANCE_CONTENT = `# SDK 合规性文档

**生效日期：{{effective_date}}**

## 说明

本文档面向将 Verhub SDK（以下简称「SDK」）集成到自有应用中的开发者（以下简称「接入方」），逐项公示 SDK 的功能、所收集的个人信息字段、系统权限使用情况以及数据的传输与处理规则，供接入方履行告知义务、在其自身的隐私政策中据实披露。本文档同时向最终用户公开。

本文档与《隐私政策》配套：本文档说明 SDK「收集什么、如何收集、如何关闭」，《隐私政策》说明这些数据到达服务端后「如何处理、留存多久、如何行使权利」。

本文档所称个人信息的判定范围，参照国家标准《GB/T 35273 信息安全技术 个人信息安全规范》附录；系统权限的判定范围，参照《网络安全实践指南——移动互联网应用程序个人信息相关系统权限申请使用规范》。

本文档将帮助您了解以下内容：

一、SDK 基本信息

二、SDK 的功能与触发方式

三、个人信息收集清单

四、系统权限使用情况

五、SDK 不收集的信息

六、数据的传输与处理规则

七、匿名标识符与事件采集的退出机制

八、各语言版本的差异

九、第三方依赖

十、接入方的合规义务

十一、合规配置指引

十二、本文档的更新

## 一、SDK 基本信息

| 项目 | 内容 |
| --- | --- |
| SDK 名称 | Verhub SDK |
| 提供形式 | 开源软件，随 Verhub 项目一并发布，由接入方自行集成 |
| 服务类型 | 版本更新检查、公告分发、用户反馈收集、日志上报、用户行为事件采集与分析 |
| 语言版本 | TypeScript / JavaScript、Python、Rust、浏览器（纯 JS） |
| 数据接收方 | 接入方在初始化时指定的 Verhub 服务实例。就本实例而言，接收方为 {{operator_name}} |
| 配套隐私政策 | 本站《隐私政策》 |

**SDK 不向 Verhub 的开发者回传任何数据。** 请求地址由接入方在初始化时指定，数据只发往该地址所指向的服务实例；Verhub 的开发者不参与该实例的运营，不接收、不存储、不访问其中的任何数据。

## 二、SDK 的功能与触发方式

| 功能 | 触发方式 | 是否必需 |
| --- | --- | --- |
| 查询项目公开信息、版本列表与版本详情 | 应用调用相应查询方法 | 否，由接入方决定是否使用 |
| 检查更新 | 应用调用检查更新方法 | 否 |
| 获取公告 | 应用调用公告查询方法 | 否 |
| 提交反馈 | 最终用户在应用内主动提交 | 否 |
| 上报日志 | 应用调用日志上报方法 | 否 |
| 上报行为事件 | 应用调用 \`track\` 方法 | 否 |

**SDK 不在后台自行运行，不定时轮询，也不在应用未调用时发起任何请求。** 未被应用调用的能力不会产生任何数据。

**除行为事件采集外，SDK 不在设备上生成、写入或者保存任何标识符，不使用 Cookie、本地存储或者本地文件留存数据；请求失败时不写入本地队列，也不在下次启动时补发。** 行为事件采集是唯一的例外：它会在设备上保存一个随机生成的匿名标识与一个待发送队列，详见第七条。该能力默认启用，但**在应用未调用 \`track\` 方法之前不会写入任何内容**，也可以在初始化时完全关闭。

## 三、个人信息收集清单

### （一）每次请求均会附带的信息

| 字段 | 内容 | 是否属于个人信息 | 收集方式 | 使用目的 | 是否可关闭 |
| --- | --- | --- | --- | --- | --- |
| 项目标识 | 接入方在服务端登记的项目标识符 | 否 | 由接入方在初始化时配置 | 确定数据归属的项目 | 否，缺少则无法路由 |
| 平台类型 | windows、linux、macos、ios、android、web、others 七者之一 | 否 | SDK 自动判定或者由接入方显式声明，通过请求头 x-verhub-platform 发送 | 按平台筛选应展示的公告；分平台统计 | 可关闭，见第十条 |
| 系统版本明细 | 操作系统版本字符串，如「11」「ubuntu 24.04」 | 否 | 同上，通过请求头 x-verhub-platform-version 发送 | 系统版本分布统计；异常时缩小排查范围 | 可关闭，见第十条 |
| SDK 标识 | SDK 名称与版本号（verhub-sdk-js、verhub-sdk-python、verhub-sdk-rust），可附带接入方设置的应用标识，通过 User-Agent 发送 | 否 | SDK 自动生成 | 区分调用来源 | 浏览器环境下不发送 |
| 网络地址 | 设备访问服务端时使用的 IP 地址 | **是** | 由网络协议本身携带，服务端可观测 | 归属地聚合统计、频次限制、故障排查 | 否，属于网络通信的固有组成部分 |

**关于系统版本明细的读取范围。** SDK 只读取操作系统的版本信息本身：Windows 通过系统接口读取内核版本号并还原为市场版本号（11、10、8.1、8、7），macOS 读取产品版本号，Linux 读取 /etc/os-release（或者 /usr/lib/os-release）中的发行版标识与版本号并拼成「发行版 版本号」，浏览器环境不读取。**除上述文件外，SDK 不读取设备上的任何文件、目录或者系统数据库。**

### （二）各功能收集的字段

**1、查询项目公开信息、版本列表、版本详情与公告**

除本条第（一）项所列的通用信息外，不发送其他信息。公告查询可附带接入方指定的平台筛选参数与分页参数。上述字段均不属于个人信息。

**2、检查更新**

| 字段 | 是否必填 | 上限 | 是否属于个人信息 | 说明 |
| --- | --- | --- | --- | --- |
| 当前版本号 | 与下一项至少提供其一 | 64 字符 | 否 | 由应用提供 |
| 用于比较的版本序号 | 可选 | 64 字符 | 否 | 由应用提供 |
| 是否包含预览版 | 可选 | — | 否 | 由应用提供 |

**3、提交反馈**

仅在最终用户在应用内主动提交时触发。

| 字段 | 是否必填 | 上限 | 是否属于个人信息 | 说明 |
| --- | --- | --- | --- | --- |
| 反馈正文 | 必填 | 4096 字符 | 取决于用户填写的内容 | 由用户输入 |
| 评分 | 可选 | 1 至 5 的整数 | 否 | 由用户选择 |
| 联系方式 | 可选 | 128 字符 | **是** | 由用户填写，可为邮箱、电话或者即时通讯账号；不做格式校验，不填写则不发送 |
| 用户标识 | 可选 | 64 字符 | **可能是** | 由接入方定义并提交，其含义由接入方决定，服务端不作解析 |
| 是否转发至 GitHub | 可选 | — | 否 | 默认为否，见下文 |
| 是否隐藏该反馈 | 可选 | — | 否 | 默认为否。设为是时，管理端列表默认不展示该条反馈，其评分仍计入统计 |
| 平台类型、系统版本明细 | 可选 | — | 否 | 可在此覆盖通用声明 |
| 自定义字段 | 可选 | — | 取决于接入方填入的内容 | 由接入方组织的结构化内容 |

**关于转发至 GitHub。** 仅当本实例为该项目启用了反馈转发功能，且用户在提交时明确选择转发，反馈正文、联系方式、用户标识、平台与系统版本才会按模板写入运营者指定的 GitHub 仓库中的一条 Issue。**若该仓库为公开仓库，上述内容将成为公开可见的信息。** 用户未选择转发的，不会发生此项传输。选择转发时联系方式为必填项，SDK 在本地即会拦下未填联系方式的提交。

**4、上报日志**

| 字段 | 是否必填 | 上限 | 是否属于个人信息 | 说明 |
| --- | --- | --- | --- | --- |
| 日志级别 | 必填 | 0 至 3 的整数 | 否 | 由应用提供 |
| 日志正文 | 必填 | 4096 字符 | 取决于接入方填入的内容 | 由应用组织 |
| 设备信息字段 | 可选 | — | 取决于接入方填入的内容 | 由应用组织的结构化内容 |
| 自定义字段 | 可选 | — | 取决于接入方填入的内容 | 由应用组织的结构化内容 |

**设备信息字段与日志正文的内容完全由接入方自行组织，SDK 不会自动读取任何设备信息填入其中。** 其中是否包含可识别到最终用户的信息，取决于接入方的实现。**这部分内容的最小化与匿名化由接入方负责：服务端按原样接收并存储，不作解析，不识别其中的个人信息，也不代为进行匿名化或者去标识化处理。**

**5、上报行为事件**

仅在应用调用 \`track\` 方法时产生。事件先进入设备上的待发送队列，攒够一批或达到发送间隔后一并上传。

| 字段 | 是否必填 | 上限 | 是否属于个人信息 | 说明 |
| --- | --- | --- | --- | --- |
| 匿名标识 | 必填 | 128 字符 | **可能是** | 由 SDK 在设备本地随机生成的 UUID，用于把同一使用者的事件串成序列。详见第七条 |
| 会话标识 | 可选 | 128 字符 | 否 | 由 SDK 随机生成，空闲 30 分钟即更换，仅存于内存 |
| 事件标识 | 必填 | 128 字符 | 否 | 由 SDK 为每条事件随机生成，用于服务端去重，不能据以识别设备 |
| 事件名 | 必填 | 64 字符 | 否 | 由应用定义，如 \`checkout_clicked\` |
| 发生时间 | 可选 | — | 否 | 由 SDK 记录，使离线补发的事件仍能落在正确的时间上 |
| 自定义属性 | 可选 | — | 取决于接入方填入的内容 | 由应用组织的结构化内容 |

**自定义属性的内容完全由接入方自行组织，SDK 不会自动读取任何设备信息填入其中。** 其中是否包含可识别到最终用户的信息，取决于接入方的实现。**这部分内容的最小化与匿名化由接入方负责：服务端按原样接收并存储，不作解析，不识别其中的个人信息，也不代为进行匿名化或者去标识化处理。**

**服务端不保存本次请求的请求头与请求体。** 除上表所列字段与本条第（三）项所述的来源信息外，不另行记录。

### （三）服务端在接收提交时另行记录的信息

反馈、日志与行为事件三类提交，服务端在落库时另行记录：IP 地址、User-Agent、由 IP 推断的地理位置（国家、省级行政区、城市）、平台与系统版本、提交时间。反馈与日志另记录一个用于抑制重复提交的指纹。

**行为事件的 IP 地址默认以匿名化形式保存**：IPv4 清零末段（如 \`203.0.113.7\` 存为 \`203.0.113.0\`），IPv6 清零末 80 位。归属地推断在匿名化之前用完整地址完成，因此地区统计的精度不受影响。运营者可通过 \`VERHUB_EVENT_IP_STORAGE\` 改为保存完整地址或者完全不保存，默认为匿名化。

其余查询类接口不生成与单次请求对应的记录，仅按「项目 × 接口 × 小时 × 平台 × 地区」累加计数。

## 四、系统权限使用情况

**Verhub SDK 不申请任何操作系统权限。**

对照《网络安全实践指南——移动互联网应用程序个人信息相关系统权限申请使用规范》，SDK 不申请下列任何一项权限：位置、通讯录、相机、麦克风、存储、电话、短信、日历、身体传感器、运动与健身、附近的设备、悬浮窗、后台自启动、关联启动。

SDK 在本机的文件操作仅限两项，均在应用自身的用户目录内，不需要申请任何权限：

1、**读取**：在 Linux 系统上读取 /etc/os-release（或者 /usr/lib/os-release）以获取发行版版本号。该文件是系统的公开版本标识文件，不含个人信息。

2、**写入与读取**：启用行为事件采集时，在用户状态目录下写入一个 JSON 文件，内容仅为 SDK 自己生成的匿名标识、退出标记与尚未发送的事件，具体路径与退出方式见第七条。**该文件由 SDK 创建，SDK 不读取该文件之外的任何用户文件。**

## 五、SDK 不收集的信息

SDK 不读取、不发送下列任何一项：

- 设备序列号、IMEI、IMSI、ICCID、MAC 地址、Android ID、IDFA、IDFV、OAID、OpenUDID 等设备唯一标识或者广告标识符；
- 通讯录、通话记录、短信、相册、摄像头、麦克风；
- 精确地理位置（卫星定位）与传感器数据；
- 剪贴板内容、键盘输入、屏幕内容；
- 应用安装列表与其他应用的运行状态；
- 文件系统的目录结构与文件内容（第四条所述的两个文件除外，其中一个是系统公开的版本标识文件，另一个是 SDK 自己写入的状态文件）；
- 账号密码等身份凭据；
- 网络运营商、SIM 卡信息、基站与 Wi-Fi 信息。

**SDK 生成的匿名标识是本地随机数，不含任何设备特征，因此不具备跨应用、跨设备识别同一使用者的能力。** 详见第七条。

## 六、数据的传输与处理规则

1、**传输。** 请求地址由接入方在初始化时指定。接入方应配置 HTTPS 地址，以保证传输过程加密。

2、**落盘。** 除行为事件采集外，SDK 不在设备上缓存、排队或者持久化任何待发送数据；这些请求失败即失败，不写入本地存储，也不在下次启动时补发。行为事件采集会在设备上保存匿名标识与待发送队列，见第七条。

3、**重试。** 仅对幂等方法（GET 等）在连接失败或者服务端返回 502、503、504 时自动重试，默认 2 次并按指数退避。**提交类请求（检查更新、反馈、日志）一律不自动重试**，不会因重试产生重复提交。

**行为事件是上一条的唯一例外**：待发送队列在发送失败后会按指数退避重试。这样做不会产生重复数据——每条事件带一个随机生成的事件标识，服务端据此精确去重，同一条事件无论重发多少次都只入库一次。

4、**页面卸载时的兜底发送（仅浏览器）。** 标签页被关闭、切到后台或者跳转离开时，尚未发送的事件会通过浏览器内建的 \`navigator.sendBeacon\` 送出，而不是随页面一起丢失。该机制由浏览器在页面消失之后完成投递，SDK 不为此保持任何后台活动；发送的内容与常规上报完全相同。用户已退出采集时不触发。

5、**超时。** 单次请求默认 15 秒超时，可由接入方调整。

6、**重复抑制。** 服务端对反馈与日志计算一个指纹，在短时间窗口（默认 60 秒）内将内容相同的重复提交折叠为一条。该指纹以 SHA-256 计算后截取 128 位，为单向散列，不能据以还原原始内容，也不用于识别设备。**行为事件不适用这种模糊抑制**：它靠上述事件标识精确去重，因为「用户连点了三次」在行为分析里是有意义的信号，不应被折叠。

7、**频次限制。** 公开上报接口按来源 IP 限流，默认为每分钟 300 次；选择转发至 GitHub 的反馈另设更严格的限制，默认为每个 IP 每小时 3 次；行使数据主体权利的自助接口（导出与删除）默认为每个 IP 每小时 10 次。实际取值由运营者配置。

8、**后续处理。** 上述数据在服务端的存储地域、留存期限、共享范围以及最终用户可以行使的权利，见《隐私政策》。

## 七、匿名标识符与事件采集的退出机制

本条只适用于行为事件采集。SDK 的其余能力不生成标识、不写入设备。

### （一）匿名标识的性质

| 项目 | 内容 |
| --- | --- |
| 生成方式 | 由 SDK 在设备本地生成的随机 UUID（版本 4）。**不读取任何设备特征**，与设备指纹是两回事 |
| 不包含 | 设备序列号、IMEI、MAC 地址、广告标识符、账号、手机号或者任何其他既有标识 |
| 存储位置 | 浏览器为 localStorage；桌面端为用户状态目录下的 \`verhub-sdk/<命名空间>.json\`——Windows 在 \`%LOCALAPPDATA%\`，macOS 在 \`~/Library/Application Support\`，Linux 在 \`$XDG_STATE_HOME\`（默认 \`~/.local/state\`）。命名空间由服务实例地址与项目标识算出，因此每个实例、每个项目各自一份，互不读写 |
| 用途 | 把同一使用者的事件串成序列，以计算漏斗转化率与留存曲线。**单条行为记录本身没有分析价值，必须能组合才能回答问题**，这是需要该标识的唯一原因 |
| 作用范围 | 按**服务实例地址 + 项目标识**隔离：同一标识不会被另一个 Verhub 实例或者另一个项目读到，跨设备也无法关联为同一人。同一实例同一项目下的两个应用如需各自独立，由接入方设置 \`namespace\` 选项 |
| 留存期限 | 随事件明细的项目保留期，默认 90 天，到期自动删除 |
| 生成时机 | **在应用首次调用 \`track\` 之前不会生成，也不会写入任何内容** |

同时保存的还有一个待发送队列（尚未上传的事件）与一个退出标记，位置同上、隔离方式同上。写入采用「先写临时文件再原子替换」，因此进程中途退出不会留下损坏的状态文件。

### （二）退出与重置

| 方法 | 效果 |
| --- | --- |
| \`optOut()\` | 立即停止采集，清空待发送队列，**删除本地的匿名标识**，并记录退出标记 |
| \`optIn()\` | 撤销退出。会生成一个**全新的**匿名标识，不复用退出前的那个——重新同意的是「从现在起」，不是「连同以前」 |
| \`hasOptedOut()\` | 查询当前是否处于退出状态 |
| \`resetIdentity()\` | 保持采集但更换匿名标识，切断与既往事件序列的关联 |
| 初始化时传 \`persistence: "none"\` | 完全不生成持久标识，也不在设备上写入任何内容 |
| 初始化时关闭采集 | 该能力整个不启用，不生成标识、不落盘、不发请求 |

**退出标记本身会写入设备。** 不写的话退出在应用重启后就失效了；保存「用户已拒绝」这一事实是执行用户选择所必需的，不属于需要另行取得同意的处理。

浏览器环境下，SDK 默认还会尊重两个通用退出信号：\`Global Privacy Control\`（\`navigator.globalPrivacyControl\`）与 \`Do Not Track\`（\`navigator.doNotTrack\` 为 \`1\`）。命中任一即等同于用户已退出，可在初始化时关闭该行为。

**用户退出后，SDK 不再发起任何事件采集请求**，而不是发一个会被服务端丢弃的请求。直接对接 HTTP 接口的接入方可以改用请求头 \`x-verhub-do-not-track: 1\`，服务端收到后同样不入库、不计数、不解析归属地。

### （三）事前同意（面向欧盟等地区）

初始化时传入 \`requireConsent\`（Python 为 \`require_consent\`，Rust 为 \`AnalyticsOptions.require_consent\`）后，SDK 在应用调用 \`grantConsent()\` 之前**完全不采集、不写盘**，包括不生成匿名标识；此期间产生的事件直接丢弃，不在内存中暂存。\`revokeConsent()\` 用于撤回同意。

**面向欧盟用户的接入方应当开启该选项。** 参见第十条第 3 项。

### （四）数据主体权利

SDK 提供两个方法供最终用户在应用内自助行使权利：

| 方法 | 权利 |
| --- | --- |
| \`exportMyData()\` | 导出本机匿名标识下的全部事件明细（访问权、可携带权） |
| \`deleteMyData()\` | 删除本机匿名标识下的全部事件明细与日活跃记录（删除权） |

接入方也可以在管理端代最终用户执行删除。删除的范围与不在范围内的部分，见《隐私政策》。

## 八、各语言版本的差异

四个语言版本的采集范围与判定规则一致，差异仅来自运行环境的固有限制。

| 能力 | TypeScript / JavaScript | Python | Rust | 浏览器（纯 JS） |
| --- | --- | --- | --- | --- |
| 平台自动判定 | 按运行时宿主系统判定；浏览器与 Worker 记作 web | 按解释器所在系统判定 | 按编译目标判定 | 一律记作 web |
| 系统版本自动读取 | 服务端运行时读取，浏览器环境不读取 | 读取 | 读取 | 不读取 |
| User-Agent 声明 | 服务端运行时发送 | 发送 | 发送 | 不发送，浏览器禁止脚本改写 |
| 关闭平台声明 | 支持 | 支持 | 支持 | 支持 |
| 单独关闭系统版本声明 | 支持 | 支持 | 不支持，关闭平台声明时一并关闭 | 支持 |
| 事件采集的退出与同意机制 | 支持全部 | 支持全部 | 支持全部 | 支持全部 |
| 匿名标识存储位置 | 用户状态目录下按实例分开的文件 | 用户状态目录下按实例分开的文件 | 用户状态目录下按实例分开的文件 | localStorage，键名按实例分开 |
| 事件按时间间隔自动发送 | 支持，后台定时器 | 支持，守护线程定时器 | **不支持后台定时器**，改为在下一次 \`track\` 时顺带检查是否已过间隔；最后一批需调用 \`flush()\` 发出 | 支持，后台定时器 |
| 页面卸载时兜底发送 | 仅浏览器环境下生效 | 环境无此概念 | 环境无此概念 | 支持，用 \`navigator.sendBeacon\` |
| 尊重 GPC / DNT 信号 | 仅浏览器环境有此信号 | 环境无此信号 | 环境无此信号 | 支持 |

浏览器环境下未声明平台与系统版本时，服务端会依据浏览器自身发送的 User-Agent 推断平台与系统版本，用于聚合统计。该请求头由浏览器固有发送，SDK 无法阻止。

## 九、第三方依赖

| 语言版本 | 第三方依赖 | 用途 | 是否收集个人信息 |
| --- | --- | --- | --- |
| TypeScript / JavaScript | 无 | 使用运行时内建的 fetch | — |
| 浏览器（纯 JS） | 无 | 使用浏览器内建的 fetch | — |
| Python | httpx | HTTP 请求 | 否 |
| Rust | reqwest、os_info、serde、serde_json、tokio、log、percent-encoding、thiserror | HTTP 请求、系统版本读取、序列化 | 否 |

**上述依赖均为通用基础库，不包含任何数据采集、广告或者统计类 SDK。** 其中 os_info 用于读取操作系统版本，在个别系统上会调用系统自带的版本查询命令，读取范围仍限于操作系统版本本身。**匿名标识由各语言的标准库生成，未为此引入任何第三方随机数或者标识库。**

## 十、接入方的合规义务

为保障接入方的应用合法合规，避免出现侵害最终用户个人信息权益的情形，接入方在使用 SDK 时应当履行下列义务：

**1、在自身隐私政策中据实披露。** 接入方应在其应用的隐私政策中列明所集成的第三方 SDK 及其收集的个人信息类型。参考条款如下，具体内容请按接入方实际启用的能力与配置调整：

| 披露项 | 内容 |
| --- | --- |
| SDK 名称 | Verhub SDK |
| 服务类型 | 版本更新检查、公告分发、用户反馈收集、日志上报、行为事件采集（按实际启用的能力填写） |
| 收集个人信息类型 | 网络信息（IP 地址）；可选——联系方式（由用户在提交反馈时自愿填写）、用户标识（由本应用定义并提交）、匿名标识（启用行为事件采集时由 SDK 在设备本地随机生成） |
| 数据接收方 | {{operator_name}} |
| 隐私政策链接 | 本站《隐私政策》页面地址 |

**2、取得同意后再初始化。** 接入方应确保在最终用户阅读并同意其隐私政策之前，不创建 SDK 客户端、不调用任何会发起网络请求的方法。

**3、面向欧盟等地区的用户，必须开启事前同意模式。** 欧盟《通用数据保护条例》（GDPR）要求行为分析类处理具备合法性基础，实践中即为用户同意；《电子隐私指令》第 5 条第 3 款进一步要求**在设备上写入或者读取信息之前**取得同意，分析用途不适用其中的「严格必要」例外。

因此，面向欧盟用户的接入方应当：

- 在初始化时传入 \`requireConsent\`（Python 为 \`require_consent\`），并且**在用户明确同意之前不调用 \`grantConsent()\`**。此前 SDK 不会生成匿名标识、不会写入设备、不会采集任何事件；
- 在其同意管理界面中提供撤回入口，接到撤回时调用 \`revokeConsent()\` 或者 \`optOut()\`；
- 在其隐私政策中说明匿名标识的用途与留存期限，并告知用户可通过应用内入口行使访问、导出与删除权利（对应 SDK 的 \`exportMyData()\` 与 \`deleteMyData()\`）。

**同意的取得方式、范围与举证责任在接入方一侧。** SDK 无从判断某一次调用是否已经取得同意；服务端同样无从判断。

**4、日志与行为事件须另行取得明确同意。** 这两项能力所上报的内容由接入方自行组织，其中可能包含可识别到最终用户的信息。**接入方应当在取得最终用户明确同意的前提下调用，并在调用前自行完成内容的最小化与匿名化处理。** 服务端按原样接收并存储，不代为解析或者匿名化。

**5、留存授权记录。** 接入方应确保已通过隐私政策等文件取得最终用户明确有效的授权，相关授权文件与授权记录需要存档备查。当最终用户或者监管机构提出授权异议时，接入方有义务提供相应记录以证明其合规使用。

**6、未成年人。** 接入方的应用面向未成年人的，应采取必要措施确保相关使用已取得监护人同意，并在其自身隐私政策中履行相应的告知义务。

**7、及时升级。** 接入方应关注 SDK 的版本更新，及时升级至最新版本。

因接入方未合规使用 SDK 所导致的风险与后果，由接入方自行承担。

## 十一、合规配置指引

**1、延迟初始化。** 在最终用户同意隐私政策之前不要创建客户端。SDK 在构造时不会发起任何网络请求，但会读取系统版本信息，因此仍建议把构造动作放在取得同意之后。

**2、关闭平台与系统版本声明。** 若接入方不希望上报平台类型与系统版本明细，可在初始化时显式关闭：

| 语言版本 | 关闭方式 |
| --- | --- |
| TypeScript / JavaScript、浏览器 | 初始化选项中传入 platform 为 null；需要保留平台声明而只关闭版本时，传入 platformVersion 为 null |
| Python | 构造参数传入 platform=None；只关闭版本时传入 platform_version=None |
| Rust | 构造器上调用 without_platform()，平台与系统版本一并不再声明 |

关闭后，服务端仍会依据 User-Agent 作出推断（浏览器环境尤其如此）。该推断仅用于聚合统计，不影响接口返回的内容。

**3、配置行为事件采集。** 该能力默认启用（设备级匿名标识 + 本地待发送队列），可按需调整：

| 目标 | 配置方式 |
| --- | --- |
| 完全关闭 | TS / 浏览器：\`analytics: { enabled: false }\`；Python：\`analytics={"enabled": False}\`；Rust：构造器上调用 \`without_analytics()\` |
| 只在同意后采集（欧盟场景） | 传入 \`requireConsent\` / \`require_consent\` / \`require_consent: true\`，并在取得同意后调用 \`grantConsent()\` |
| 不在设备上保存任何内容 | \`persistence\` 传 \`"none"\`；此时事件仍可上报，但无法做任何按人的分析 |
| 标识只在本次运行内有效 | \`persistence\` 传 \`"session"\`；漏斗仍可用，跨天留存无法计算 |
| 调整发送节奏 | \`flushIntervalMs\` / \`batchSize\` / \`maxQueueSize\` |

**4、退出前记得发送。** 攒在队列里但还没到发送间隔的事件会随进程一起消失。应用退出前调用一次 \`flush()\`（Python 的 \`close()\` 与 Rust 的 \`flush()\` 同理）即可。

**5、按需启用能力。** SDK 的各项能力互相独立，未调用的能力不会产生任何数据。只需要版本检查与公告的接入方，无需调用反馈、日志与行为事件相关的方法。

## 十二、本文档的更新

SDK 的采集范围、字段或者处理规则发生变化时，本文档将同步修订。修订后的版本自在本页面发布之日起适用，页面底部标注最后更新日期。

如您对本文档有任何疑问，可以通过 {{contact_email}} 与我们联系。
`
