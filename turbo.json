{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": [
    "NODE_ENV",
    "PORT",
    "DATABASE_URL",
    "JWT_SECRET",
    "JWT_EXPIRES_IN",
    "ADMIN_PASSWORD",
    "BOOTSTRAP_SECRET_DIR",
    "API_KEY_SALT",
    "VERHUB_GEO_ENABLED",
    "VERHUB_GEO_PROVIDERS",
    "VERHUB_GEO_TTL_DAYS",
    "VERHUB_GEO_TIMEOUT_MS",
    "VERHUB_DEDUP_WINDOW_SECONDS"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
